# Search and corridor construction

This package owns topology seed generation, time-expanded visibility search,
bounded time layers, swept envelopes, route corridors, and corridor
certificates. It consumes normalized inputs and canonical obstacle geometry;
it does not own request parsing, HS3 optimization, final validation, or
plotting.
