# Geometry

This package owns canonical boundary-to-`polyshape` conversion, deterministic
boundary traversal, convex decomposition, and signed polygon clearance.
Search, obstacle interpolation, validation, querying, and plotting use these
primitives so protected geometry has one interpretation.
