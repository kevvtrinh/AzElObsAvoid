function [profile, candidates] = createFixedTimeAxisProfile( ...
        initialState, terminalState, limits, duration)
%% Section 0: Header & Readme
% SYNTAX
%   profile = ruckigEngine.createFixedTimeAxisProfile( ...
%       initialState, terminalState, limits, duration)
%   [profile, candidates] = ruckigEngine.createFixedTimeAxisProfile( ...
%       initialState, terminalState, limits, duration)
%**************************************************************************
% PURPOSE
%   - Create a path-short scalar jerk-switching profile at a prescribed time.
%**************************************************************************
% INPUTS
%   - initialState (scalar struct)
%       Scalar position, velocity, and acceleration fields.
%   - terminalState (scalar struct)
%       Scalar position, velocity, and acceleration fields.
%   - limits (scalar struct)
%       Positive maximumVelocity, maximumAcceleration, and maximumJerk.
%   - duration (positive finite scalar)
%       Required motion duration.
%**************************************************************************
% OUTPUTS
%   - profile (scalar struct)
%       Success, phase law, exact boundary histories, path length, and family.
%       Unsupported numerical edge cases return Success = false.
%   - candidates (structure array)
%       Every certified fixed-time family for multidimensional path ranking.
%**************************************************************************
% UNITS
%   - Time and coordinate units are caller-defined and must be consistent.
%**************************************************************************

% The switching equations are adapted from Ruckig v0.19.4 under its MIT
% license; see trajectory/THIRD_PARTY_NOTICES.txt.

%% Section 1: Create Fixed-Time Switching Candidates

context = createContext(initialState, terminalState, limits, duration);
candidates = repmat(createEmptyProfile(), 0, 1);
% A zero-jerk phase is the exact least-effort solution when the supplied
% endpoint states already lie on one constant-acceleration trajectory.
candidates = appendEvaluated(candidates, initialState, terminalState, ...
    limits, duration, 0, "constantAcceleration");
minimumProfile = ruckigEngine.createMinimumTimeAxisProfile( ...
    initialState, terminalState, limits);
if minimumProfile.Success && duration >= minimumProfile.Duration
    dwellDuration = duration - minimumProfile.Duration;
    if isRest(initialState)
        candidates = appendEvaluated(candidates, initialState, terminalState, ...
            limits, [dwellDuration, minimumProfile.PhaseDuration], ...
            [0, minimumProfile.PhaseJerk], "initialDwell");
    end
    if isRest(terminalState)
        candidates = appendEvaluated(candidates, initialState, terminalState, ...
            limits, [minimumProfile.PhaseDuration, dwellDuration], ...
            [minimumProfile.PhaseJerk, 0], "terminalDwell");
    end
end
for direction = [1, -1]
    directed = createDirectedLimits(context, direction);
    candidates = appendAccelerationVelocityProfiles( ...
        candidates, context, directed);
    candidates = appendTerminalAccelerationVelocityProfiles( ...
        candidates, context, directed);
    candidates = appendVelocityProfiles(candidates, context, directed);
end

%% Section 2: Select The Shortest Spatial Profile

profile = createEmptyProfile();
if isempty(candidates)
    profile.Message = ...
        "No fixed-time jerk-switching family satisfied the boundary states.";
    return;
end

pathLength = [candidates.PathLength];
integratedSquaredJerk = zeros(numel(candidates), 1);
for candidateIndex = 1:numel(candidates)
    integratedSquaredJerk(candidateIndex) = sum( ...
        candidates(candidateIndex).PhaseJerk .^ 2 .* ...
        candidates(candidateIndex).PhaseDuration);
end
shortestPathLength = min(pathLength);
pathTieTolerance = 1e-10 * max(1, shortestPathLength);
pathRanking = pathLength;
pathRanking(pathLength <= shortestPathLength + pathTieTolerance) = ...
    shortestPathLength;
ranking = [pathRanking(:), integratedSquaredJerk(:)];
[~, order] = sortrows(ranking, [1, 2]);
candidates = candidates(order);
profile = candidates(1);
profile.Message = "A path-short fixed-time axis profile was created.";
end

%% Section 3: Local Functions

function context = createContext(initialState, terminalState, limits, duration)
% Cache boundary values and powers used by the synchronized equations.
context = struct( ...
    "InitialState", initialState, ...
    "TerminalState", terminalState, ...
    "Limits", limits, ...
    "duration", duration, ...
    "p0", initialState.position, ...
    "pf", terminalState.position, ...
    "v0", initialState.velocity, ...
    "vf", terminalState.velocity, ...
    "a0", initialState.acceleration, ...
    "af", terminalState.acceleration, ...
    "displacement", terminalState.position - initialState.position);
context.velocityDifference = context.vf - context.v0;
context.accelerationDifference = context.af - context.a0;
context.v0Squared = context.v0^2;
context.vfSquared = context.vf^2;
context.a0Squared = context.a0^2;
context.afSquared = context.af^2;
context.a0Cubed = context.a0^3;
context.afCubed = context.af^3;
context.a0Fourth = context.a0^4;
context.afFourth = context.af^4;
end

function directed = createDirectedLimits(context, direction)
% Reverse every signed bound together so one equation set covers both ways.
directed = struct( ...
    "vMaximum", direction * context.Limits.maximumVelocity, ...
    "vMinimum", -direction * context.Limits.maximumVelocity, ...
    "aMaximum", direction * context.Limits.maximumAcceleration, ...
    "aMinimum", -direction * context.Limits.maximumAcceleration, ...
    "jMaximum", direction * context.Limits.maximumJerk);
end

function candidates = appendAccelerationVelocityProfiles( ...
        candidates, context, limits)
% Add UDDU and UDUD profiles that touch both acceleration bounds and velocity.
aMaximum = limits.aMaximum;
aMinimum = limits.aMinimum;
jMaximum = limits.jMaximum;
duration = context.duration;
jSquared = jMaximum^2;

if (2 * (aMaximum - aMinimum) + ...
        context.accelerationDifference) / jMaximum < duration
    radical = (context.a0Fourth + context.afFourth - ...
        4 * context.a0Cubed * (2 * aMaximum + aMinimum) / 3 - ...
        4 * context.afCubed * (aMaximum + 2 * aMinimum) / 3 + ...
        2 * (context.a0Squared - context.afSquared) * aMaximum^2 + ...
        (4 * context.a0 * aMaximum - 2 * context.a0Squared) * ...
        (context.afSquared - 2 * context.af * aMinimum + ...
        (aMinimum - aMaximum) * aMinimum + 2 * jMaximum * ...
        (aMinimum * duration - context.velocityDifference)) + ...
        2 * context.afSquared * (aMinimum^2 + 2 * jMaximum * ...
        (aMaximum * duration - context.velocityDifference)) + ...
        4 * jMaximum * (2 * aMinimum * ...
        (context.af * context.velocityDifference + jMaximum * ...
        (-context.displacement + duration * context.v0)) + ...
        (aMaximum^2 - aMinimum^2) * context.velocityDifference + ...
        jMaximum * context.velocityDifference^2) + ...
        8 * aMaximum * jSquared * ...
        (context.displacement - duration * context.vf)) / ...
        (aMaximum * aMinimum) + 4 * context.afSquared + ...
        2 * context.a0Squared + ...
        (4 * context.af + aMaximum - aMinimum) * ...
        (aMaximum - aMinimum) + 4 * jMaximum * ...
        (aMinimum - aMaximum + jMaximum * duration - ...
        2 * context.af) * duration;
    root = ruckigEngine.internal.safeSqrt(radical) * ...
        abs(jMaximum) / jMaximum;
    if isfinite(root)
        phase = zeros(1, 7);
        phase(1) = (-context.a0 + aMaximum) / jMaximum;
        phase(2) = (-(context.afSquared - context.a0Squared + ...
            2 * aMaximum^2 + aMinimum * ...
            (aMinimum - 2 * context.accelerationDifference - ...
            3 * aMaximum) + 2 * jMaximum * ...
            (aMinimum * duration - context.velocityDifference)) + ...
            aMinimum * root) / ...
            (2 * (aMaximum - aMinimum) * jMaximum);
        phase(3) = aMaximum / jMaximum;
        phase(4) = (aMinimum - aMaximum + root) / (2 * jMaximum);
        phase(5) = -aMinimum / jMaximum;
        phase(6) = duration - sum(phase(1:4)) - ...
            2 * phase(5) - context.af / jMaximum;
        phase(7) = phase(5) + context.af / jMaximum;
        candidates = appendEvaluated(candidates, context.InitialState, ...
            context.TerminalState, context.Limits, phase, ...
            jMaximum * [1, 0, -1, 0, -1, 0, 1], ...
            "synchronizedAccelerationVelocity");
    end
end

if (-context.a0 + 4 * aMaximum - context.af) / jMaximum < duration
    phase = zeros(1, 7);
    phase(1) = (-context.a0 + aMaximum) / jMaximum;
    numerator = 3 * (context.a0Fourth + context.afFourth) - ...
        4 * (context.a0Cubed + context.afCubed) * aMaximum - ...
        4 * context.afCubed * aMaximum + ...
        24 * (context.a0 + context.af) * aMaximum^3 - ...
        6 * (context.afSquared + context.a0Squared) * ...
        (aMaximum^2 - 2 * jMaximum * context.velocityDifference) + ...
        6 * context.a0Squared * (context.afSquared - ...
        2 * context.af * aMaximum - 2 * aMaximum * jMaximum * duration) - ...
        12 * aMaximum^2 * (2 * aMaximum^2 - ...
        2 * aMaximum * jMaximum * duration + ...
        jMaximum * context.velocityDifference) - ...
        24 * context.af * aMaximum * jMaximum * ...
        context.velocityDifference + 12 * jSquared * ...
        (2 * aMaximum * (-context.displacement + duration * context.v0) + ...
        context.velocityDifference^2);
    denominator = 12 * aMaximum * jMaximum * ...
        (context.a0Squared + context.afSquared - ...
        2 * (context.a0 + context.af) * aMaximum + ...
        2 * (aMaximum^2 - aMaximum * jMaximum * duration + ...
        jMaximum * context.velocityDifference));
    phase(2) = numerator / denominator;
    phase(3) = aMaximum / jMaximum;
    phase(4) = (-context.a0Squared - context.afSquared + ...
        2 * aMaximum * (context.a0 + context.af - 2 * aMaximum) - ...
        2 * jMaximum * context.velocityDifference) / ...
        (2 * aMaximum * jMaximum) + duration;
    phase(5) = phase(3);
    phase(6) = duration - sum(phase(1:4)) - ...
        2 * phase(5) + context.af / jMaximum;
    phase(7) = phase(5) - context.af / jMaximum;
    candidates = appendEvaluated(candidates, context.InitialState, ...
        context.TerminalState, context.Limits, phase, ...
        jMaximum * [1, 0, -1, 0, 1, 0, -1], ...
        "synchronizedAlternatingAccelerationVelocity");
end
end

function candidates = appendTerminalAccelerationVelocityProfiles( ...
        candidates, context, limits)
% Add fixed-time profiles whose terminal-side acceleration bound is active.
jMaximum = limits.jMaximum;
aMaximum = limits.aMaximum;
aMinimum = limits.aMinimum;
duration = context.duration;

lower = -context.a0 / jMaximum;
upper = min((duration + 2 * aMinimum / jMaximum - ...
    (context.a0 + context.af) / jMaximum) / 2, ...
    (aMaximum - context.a0) / jMaximum);
rootsFound = findProfileRoots(@createUdduPhase, lower, upper, ...
    context, limits, "UDDU");
for rootIndex = 1:numel(rootsFound)
    phase = createUdduPhase(rootsFound(rootIndex), context, limits);
    candidates = appendEvaluated(candidates, context.InitialState, ...
        context.TerminalState, context.Limits, phase, ...
        jMaximum * [1, 0, -1, 0, -1, 0, 1], ...
        "synchronizedTerminalAccelerationVelocity");
end

upper = min((duration + context.accelerationDifference / jMaximum - ...
    2 * aMaximum / jMaximum) / 2, ...
    (aMaximum - context.a0) / jMaximum);
rootsFound = findProfileRoots(@createUdudPhase, lower, upper, ...
    context, limits, "UDUD");
for rootIndex = 1:numel(rootsFound)
    phase = createUdudPhase(rootsFound(rootIndex), context, limits);
    candidates = appendEvaluated(candidates, context.InitialState, ...
        context.TerminalState, context.Limits, phase, ...
        jMaximum * [1, 0, -1, 0, 1, 0, -1], ...
        "synchronizedAlternatingTerminalAccelerationVelocity");
end
end

function phase = createUdduPhase(time, context, limits)
% Complete the terminal-acceleration UDDU family from its first ramp time.
jMaximum = limits.jMaximum;
aMinimum = limits.aMinimum;
h1 = -((context.a0Squared + context.afSquared) / 2 + ...
    jMaximum * (-context.velocityDifference + ...
    2 * context.a0 * time + jMaximum * time^2)) / aMinimum;
phase = [time, 0, context.a0 / jMaximum + time, ...
    context.duration - ...
    (h1 - aMinimum + context.a0 + context.af) / jMaximum - 2 * time, ...
    -aMinimum / jMaximum, (h1 + aMinimum) / jMaximum, ...
    -aMinimum / jMaximum + context.af / jMaximum];
end

function phase = createUdudPhase(time, context, limits)
% Complete the terminal-acceleration UDUD family from its first ramp time.
jMaximum = limits.jMaximum;
aMaximum = limits.aMaximum;
h1 = ((context.a0Squared - context.afSquared) / 2 + ...
    jMaximum^2 * time^2 - jMaximum * ...
    (context.velocityDifference - 2 * context.a0 * time)) / aMaximum;
phase = [time, 0, time + context.a0 / jMaximum, ...
    context.duration + ...
    (h1 + context.accelerationDifference - aMaximum) / jMaximum - ...
    2 * time, aMaximum / jMaximum, -(h1 + aMaximum) / jMaximum, ...
    aMaximum / jMaximum - context.af / jMaximum];
end

function candidates = appendVelocityProfiles(candidates, context, limits)
% Add fixed-time UDDU and UDUD families with a velocity plateau.
jMaximum = limits.jMaximum;
aMaximum = limits.aMaximum;
lower = max(0, -context.a0 / jMaximum);
upper = min((context.duration - context.a0 / jMaximum) / 2, ...
    (aMaximum - context.a0) / jMaximum);

rootsFound = findProfileRoots(@createVelocityUdduPhase, lower, upper, ...
    context, limits, "UDDU");
for rootIndex = 1:numel(rootsFound)
    phase = createVelocityUdduPhase(rootsFound(rootIndex), context, limits);
    candidates = appendEvaluated(candidates, context.InitialState, ...
        context.TerminalState, context.Limits, phase, ...
        jMaximum * [1, 0, -1, 0, -1, 0, 1], ...
        "synchronizedVelocity");
end

rootsFound = findProfileRoots(@createVelocityUdudPhase, lower, upper, ...
    context, limits, "UDUD");
for rootIndex = 1:numel(rootsFound)
    phase = createVelocityUdudPhase(rootsFound(rootIndex), context, limits);
    candidates = appendEvaluated(candidates, context.InitialState, ...
        context.TerminalState, context.Limits, phase, ...
        jMaximum * [1, 0, -1, 0, 1, 0, -1], ...
        "synchronizedAlternatingVelocity");
end
end

function phase = createVelocityUdduPhase(time, context, limits)
% Complete the UDDU velocity family from its first ramp time.
jMaximum = limits.jMaximum;
radicand = (context.a0Squared + context.afSquared) / (2 * jMaximum^2) + ...
    (2 * context.a0 * time + jMaximum * time^2 - ...
    context.velocityDifference) / jMaximum;
h1 = ruckigEngine.internal.safeSqrt(radicand);
phase = [time, 0, time + context.a0 / jMaximum, ...
    context.duration - 2 * (time + h1) - ...
    (context.a0 + context.af) / jMaximum, ...
    h1, 0, h1 + context.af / jMaximum];
end

function phase = createVelocityUdudPhase(time, context, limits)
% Complete the UDUD velocity family from its first ramp time.
jMaximum = limits.jMaximum;
radicand = (context.afSquared - context.a0Squared) / (2 * jMaximum^2) - ...
    ((2 * context.a0 + jMaximum * time) * time - ...
    context.velocityDifference) / jMaximum;
h1 = ruckigEngine.internal.safeSqrt(radicand);
phase = [time, 0, time + context.a0 / jMaximum, ...
    context.duration - 2 * (time + h1) + ...
    context.accelerationDifference / jMaximum, ...
    h1, 0, h1 - context.af / jMaximum];
end

function rootsFound = findProfileRoots( ...
        phaseFunction, lower, upper, context, limits, controlSigns)
% Bracket all endpoint-position roots of one one-parameter switching family.
if ~isfinite(lower) || ~isfinite(upper) || upper < lower
    rootsFound = zeros(1, 0);
    return;
end
sampleCount = 65;
sample = linspace(lower, upper, sampleCount);
residual = NaN(size(sample));
rootsFound = zeros(1, 2 * sampleCount + 1);
rootCount = 0;
for sampleIndex = 1:sampleCount
    residual(sampleIndex) = positionResidual( ...
        sample(sampleIndex), phaseFunction, context, limits, controlSigns);
end
scale = max(1, abs(context.displacement));
zeroTolerance = 1e-9 * scale;
for sampleIndex = 1:(sampleCount - 1)
    leftValue = residual(sampleIndex);
    rightValue = residual(sampleIndex + 1);
    if isfinite(leftValue) && abs(leftValue) <= zeroTolerance
        rootCount = rootCount + 1;
        rootsFound(rootCount) = sample(sampleIndex);
    end
    if ~isfinite(leftValue) || ~isfinite(rightValue) || ...
            sign(leftValue) == sign(rightValue)
        continue;
    end
    root = fzero(@(time) positionResidual( ...
        time, phaseFunction, context, limits, controlSigns), ...
        sample(sampleIndex:sampleIndex + 1));
    rootCount = rootCount + 1;
    rootsFound(rootCount) = root;
end
if isfinite(residual(end)) && abs(residual(end)) <= zeroTolerance
    rootCount = rootCount + 1;
    rootsFound(rootCount) = sample(end);
end
rootsFound = rootsFound(1:rootCount);
rootsFound = unique(round(rootsFound, 12));
end

function residual = positionResidual( ...
        time, phaseFunction, context, limits, controlSigns)
% Integrate one provisional phase law and return its terminal position error.
phase = phaseFunction(time, context, limits);
if any(~isfinite(phase)) || any(phase < -1e-9)
    residual = NaN;
    return;
end
if controlSigns == "UDDU"
    jerk = limits.jMaximum * [1, 0, -1, 0, -1, 0, 1];
else
    jerk = limits.jMaximum * [1, 0, -1, 0, 1, 0, -1];
end
position = context.p0;
velocity = context.v0;
acceleration = context.a0;
for phaseIndex = 1:7
    duration = phase(phaseIndex);
    position = position + duration * ...
        (velocity + duration * ...
        (acceleration / 2 + duration * jerk(phaseIndex) / 6));
    velocity = velocity + duration * ...
        (acceleration + duration * jerk(phaseIndex) / 2);
    acceleration = acceleration + duration * jerk(phaseIndex);
end
residual = position - context.pf;
end

function candidates = appendEvaluated(candidates, initialState, ...
        terminalState, limits, phaseDuration, phaseJerk, family)
% Append one candidate only after exact integration and continuous checks pass.
candidate = ruckigEngine.evaluateAxisSwitchingProfile( ...
    initialState, terminalState, limits, ...
    phaseDuration, phaseJerk, family);
if candidate.Success
    candidate.Message = "";
    candidate = orderfields(candidate, createEmptyProfile());
    candidates(end + 1, 1) = candidate;
end
end

function value = isRest(state)
% A dwell is admissible only when it leaves position and motion state unchanged.
tolerance = 64 * eps(max([1, abs(state.velocity), abs(state.acceleration)]));
value = abs(state.velocity) <= tolerance && ...
    abs(state.acceleration) <= tolerance;
end

function profile = createEmptyProfile()
% Define stable fields for a valid candidate or fixed-time fallback request.
profile = struct( ...
    "Success", false, ...
    "Message", "No fixed-time axis profile was created.", ...
    "PhaseDuration", zeros(1, 0), ...
    "PhaseJerk", zeros(1, 0), ...
    "Duration", NaN, ...
    "Position", zeros(1, 0), ...
    "Velocity", zeros(1, 0), ...
    "Acceleration", zeros(1, 0), ...
    "Family", "", ...
    "EndpointError", Inf, ...
    "MaximumVelocity", Inf, ...
    "MaximumAcceleration", Inf, ...
    "PathLength", Inf);
end
